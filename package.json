{
  "name": "partial_fetch_ts",
  "version": "0.8.0",
  "description": "partial fetch response middleware",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ms2sato",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.0.0",
    "@types/pug": "^2.0.5",
    "typescript": "^4.3.5"
  },
  "optionalDependencies": {
    "pug": "^3.0.2"
  }
}
